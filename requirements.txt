# Core Dependencies (MVP)
pyyaml>=6.0          # Config file parsing
pydantic>=2.0        # Data validation and models
requests>=2.28.0     # USDA API client (used by ingestion layer)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Development Dependencies
pytest>=7.0          # Testing framework
pytest-cov>=4.0      # Test coverage
black>=23.0          # Code formatting
mypy>=1.0            # Type checking

# Future Dependencies (commented out for MVP)
# openai>=1.0        # LLM API (when adding LLM reasoning)
# anthropic>=0.3     # Alternative LLM API
# sentence-transformers>=2.0  # Embeddings (when adding semantic search)
# numpy>=1.24        # Numerical operations (if needed)
# pandas>=2.0        # Data manipulation (if needed)

